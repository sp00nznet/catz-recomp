# Original game binaries (copyrighted — do not redistribute)
game/

# Generated build artifacts
build/
build_data/*.bin

# IDA databases
*.i64
*.idb
*.id0
*.id1
*.id2
*.nam
*.til

# Python
__pycache__/
*.pyc

# Editor
*.swp
.DS_Store
build_wx/
build_tr/
build_sp/

# Diagnostic build variants (watchdog / sp-watch / dispatch-trace)
build_*/

# Diagnostic surface dumps (CATZ_DUMP_BLIT)
*.bmp

# Lifted output. This is a machine translation of the retail game's own code --
# a derivative work of CATZDLL.DLL and CATZ.WAD -- so it is not redistributable
# and is not shipped here. Regenerate it from your own copy of the game; see
# "Building" in README.md.
src/
